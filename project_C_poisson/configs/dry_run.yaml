# DRY RUN CONFIGURATION
hh:
  N: 100
  gL: 0.3
  gA: 20.0
  density: 0.2

task:
  dt: 0.05
  symbol_ms: 20.0
  poisson_rate_min: 10.0
  poisson_rate_max: 150.0
  xor_delay: 2
  narma_order: 10
  mc_max_lag: 20

# Execution Control
sweep_type: "phase1"  # Uses the Phase 1 logic path
results_dir: "results_dryrun"
cache_dir: "cache_dryrun"

# Minimal Grids for 1 Trial
N_grid: [100]
gL_grid: [0.3]
rho_grid_phase1: [1.0]
bias_grid_phase1: [5.0]
seeds_phase1: 1

# Minimal difficulty
difficulty_levels:
  - {xor_delay: 2, narma_order: 5, mc_max_lag: 10, label: "dry_run_easy"}
